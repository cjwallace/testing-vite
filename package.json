{
  "name": "testing-vite",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/ui": "^0.18.0",
    "fast-check": "^3.0.1",
    "typescript": "^4.5.4",
    "vite": "^2.9.9",
    "vitest": "^0.18.0"
  }
}
